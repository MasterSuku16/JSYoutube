//OPERADORES
const varible = 'valor de variable' //operador de asignacion

const suma = 1 + 2 //operador de suma
const resta = 5 - 2 //operador de resta
const multiplicacion = 1 * 3 //operador de multiplicacion
const division = 1 / 2 //operador de division
const resto = 5 % 2 // operador para sacar el resto
const exponente = 3 ** 3 //operador para sacar exponente


let x = 5;
let y = 5;

x++;
y--;

console.log(suma);
console.log(multiplicacion);